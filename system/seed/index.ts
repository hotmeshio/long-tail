// System seed module.
// MCP server definitions and workflow configs are now declared inline
// on their factories/workers in system/index.ts and seeded at startup
// via insert-if-absent. This module is retained for backward compatibility
// but no longer performs seeding.
