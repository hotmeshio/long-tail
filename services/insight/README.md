LLM-powered insight service. Invokes MCP queries and generates workflow descriptions using language models.

Key files:
- `index.ts` — `insightQuery()` entry point for scoped MCP tool invocation and result summarization
- `prompts.ts` — Externalized LLM prompts for insight generation
