// The renderer lives in resolver-form/ (orchestrator, field rows, section
// layout, dictionary lists). This path is kept so existing imports resolve.
export { ResolverForm } from './resolver-form';
