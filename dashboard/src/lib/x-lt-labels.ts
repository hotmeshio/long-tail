// Isomorphic implementation lives in shared/form-validation — the same reader
// governs the configurable labels on the escalation footer's action controls.
export * from '../../../shared/form-validation/x-lt-labels';
