// Isomorphic implementation lives in shared/form-validation — the invoke API
// gate validates against the same context the form renders with.
export * from '../../../shared/form-validation/invoke-context';
