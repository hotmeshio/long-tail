// Isomorphic implementation lives in shared/form-validation — labels resolve
// identically wherever a schema field faces a human.
export * from '../../../shared/form-validation/derive-field-label';
