// Isomorphic implementation lives in shared/form-validation — the same mapping
// drives the escalation-list embed, x-lt-actions, and the x-lt-submit-guard
// precondition on both the dashboard and the API server.
export * from '../../../shared/form-validation/x-lt-query';
