// Isomorphic implementation lives in shared/form-validation — the API layer
// resolves the same interpolated context paths for server-side enforcement.
export * from '../../../shared/form-validation/ctx-path';
