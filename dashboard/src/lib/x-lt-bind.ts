// Isomorphic implementation lives in shared/form-validation — the API layer
// runs the same bind mapping for server-side schema enforcement.
export * from '../../../shared/form-validation/x-lt-bind';
